# promo-codes-api

## Первый запуск

```bash
npm run setup
```

При необходимости указать подключение через переменные окружения:
`DB_HOST`, `DB_PORT`, `DB_USERNAME`, `DB_PASSWORD`, `DB_NAME`.

Приложение запустится на `http://localhost:3000`. Таблицы создаются автоматически.
